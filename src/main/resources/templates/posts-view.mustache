{{>layout/header}}
<div class="layer">
    <div id="view-wrapper">
        <p id="viewTitle">{{post.title}}</p>
        <div id="postView_author">{{post.author}}</div>
        {{#hasRole}}
            <button id="update-view-btn" onclick="location.href='/posts/update/{{post.id}}'">수정</button>
        {{/hasRole}}
        <div id="view-content">{{{post.content}}}</div>
    </div>
</div>
{{>layout/footer}}